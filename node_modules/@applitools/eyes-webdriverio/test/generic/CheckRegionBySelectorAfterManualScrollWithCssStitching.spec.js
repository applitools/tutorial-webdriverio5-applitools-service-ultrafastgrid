// check region by selector after manual scroll with css stitching
const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/spec-driver'))
const {testSetup, getTestInfo} = require('@applitools/sdk-shared')

describe('Coverage Tests', () => {
  let driver, destroyDriver, eyes
  beforeEach(async () => {
    ;[driver, destroyDriver] = await spec.build({browser: 'chrome'})
    eyes = testSetup.getEyes({
      displayName:
        'check region by selector after manual scroll with css stitching',
      stitchMode: 'CSS',
      baselineName: 'TestCheckRegionBySelectorAfterManualScroll_Fluent',
    })
  })
  afterEach(async () => {
    await destroyDriver(driver)
    await eyes.abort()
  })
  it('check region by selector after manual scroll with css stitching', async () => {
    await spec.visit(
      driver,
      'https://applitools.github.io/demo/TestPages/FramesTestPage/'
    )
    await eyes.open(
      driver,
      'Eyes Selenium SDK - Fluent API',
      'TestCheckRegionBySelectorAfterManualScroll_Fluent',
      {width: 700, height: 460}
    )
    await spec.executeScript(driver, 'window.scrollBy(0, 250)', ...[])
    await eyes.check({region: '#centered'})
    await eyes.close(undefined)
  })
})
