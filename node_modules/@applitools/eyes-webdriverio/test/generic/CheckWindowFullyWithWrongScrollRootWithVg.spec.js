// check window fully with wrong scroll root with vg
const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/spec-driver'))
const {testSetup, getTestInfo} = require('@applitools/sdk-shared')

describe('Coverage Tests', () => {
  let driver, destroyDriver, eyes
  beforeEach(async () => {
    ;[driver, destroyDriver] = await spec.build({browser: 'chrome'})
    eyes = testSetup.getEyes({
      vg: true,
      displayName: 'check window fully with wrong scroll root with vg',
      baselineName: 'TestCheckWindow_Html_VG',
    })
  })
  afterEach(async () => {
    await destroyDriver(driver)
    await eyes.abort()
  })
  it('check window fully with wrong scroll root with vg', async () => {
    await spec.visit(
      driver,
      'https://applitools.github.io/demo/TestPages/SimpleTestPage/scrollablebody.html'
    )
    await eyes.open(
      driver,
      'Eyes Selenium SDK - Scroll Root Element',
      'TestCheckWindow_Html_VG',
      {width: 700, height: 460}
    )
    await eyes.check({scrollRootElement: 'html', isFully: true})
    await eyes.close(undefined)
  })
})
