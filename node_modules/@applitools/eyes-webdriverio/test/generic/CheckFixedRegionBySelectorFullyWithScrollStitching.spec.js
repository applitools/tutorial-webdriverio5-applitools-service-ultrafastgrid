// check fixed region by selector fully with scroll stitching
const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/spec-driver'))
const {testSetup, getTestInfo} = require('@applitools/sdk-shared')

describe('Coverage Tests', () => {
  let driver, destroyDriver, eyes
  beforeEach(async () => {
    ;[driver, destroyDriver] = await spec.build({browser: 'chrome'})
    eyes = testSetup.getEyes({
      displayName: 'check fixed region by selector fully with scroll stitching',
      stitchMode: 'Scroll',
      baselineName: 'TestCheckFixedRegion_Fully_Scroll',
    })
  })
  afterEach(async () => {
    await destroyDriver(driver)
    await eyes.abort()
  })
  it('check fixed region by selector fully with scroll stitching', async () => {
    await spec.visit(
      driver,
      'http://applitools.github.io/demo/TestPages/fixed-position'
    )
    await eyes.open(
      driver,
      'Eyes Selenium SDK - Fluent API',
      'TestCheckFixedRegion_Fully_Scroll',
      {width: 700, height: 460}
    )
    await eyes.check({region: '#fixed', isFully: true})
    await eyes.close(undefined)
  })
})
