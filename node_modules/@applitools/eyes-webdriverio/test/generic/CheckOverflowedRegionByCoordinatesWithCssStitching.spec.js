// check overflowed region by coordinates with css stitching
const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/spec-driver'))
const {testSetup, getTestInfo} = require('@applitools/sdk-shared')

describe.skip('Coverage Tests', () => {
  let driver, destroyDriver, eyes
  beforeEach(async () => {
    ;[driver, destroyDriver] = await spec.build({browser: 'chrome'})
    eyes = testSetup.getEyes({
      displayName: 'check overflowed region by coordinates with css stitching',
      stitchMode: 'CSS',
      baselineName: 'TestCheckOverflowingRegionByCoordinates_Fluent',
    })
  })
  afterEach(async () => {
    await destroyDriver(driver)
    await eyes.abort()
  })
  it('check overflowed region by coordinates with css stitching', async () => {
    await spec.visit(
      driver,
      'https://applitools.github.io/demo/TestPages/FramesTestPage/'
    )
    await eyes.open(
      driver,
      'Eyes Selenium SDK - Fluent API',
      'TestCheckOverflowingRegionByCoordinates_Fluent',
      {width: 700, height: 460}
    )
    await eyes.check({region: {left: 50, top: 110, width: 90, height: 550}})
    await eyes.close(undefined)
  })
})
