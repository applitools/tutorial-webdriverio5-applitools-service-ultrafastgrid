// check window fully on page with sticky header with css stitching
const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/spec-driver'))
const {testSetup, getTestInfo} = require('@applitools/sdk-shared')

describe('Coverage Tests', () => {
  let driver, destroyDriver, eyes
  beforeEach(async () => {
    ;[driver, destroyDriver] = await spec.build({browser: 'chrome'})
    eyes = testSetup.getEyes({
      displayName:
        'check window fully on page with sticky header with css stitching',
      stitchMode: 'CSS',
      baselineName: 'TestCheckPageWithHeader_Window_Fully',
    })
  })
  afterEach(async () => {
    await destroyDriver(driver)
    await eyes.abort()
  })
  it('check window fully on page with sticky header with css stitching', async () => {
    await spec.visit(
      driver,
      'https://applitools.github.io/demo/TestPages/PageWithHeader/index.html'
    )
    await eyes.open(
      driver,
      'Eyes Selenium SDK - Page With Header',
      'TestCheckPageWithHeader_Window_Fully',
      {width: 700, height: 460}
    )
    await eyes.check({isFully: true})
    await eyes.close(undefined)
  })
})
