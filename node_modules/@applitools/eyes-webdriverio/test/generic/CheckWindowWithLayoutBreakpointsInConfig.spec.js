// check window with layout breakpoints in config
const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/spec-driver'))
const {testSetup, getTestInfo} = require('@applitools/sdk-shared')

describe('Coverage Tests', () => {
  let driver, destroyDriver, eyes
  beforeEach(async () => {
    ;[driver, destroyDriver] = await spec.build({browser: 'chrome'})
    eyes = testSetup.getEyes({
      vg: true,
      displayName: 'check window with layout breakpoints in config',
      browsersInfo: [
        {name: 'chrome', width: 1000, height: 800},
        {iosDeviceInfo: {deviceName: 'iPad (7th generation)'}},
        {chromeEmulationInfo: {deviceName: 'Pixel 4 XL'}},
      ],
      layoutBreakpoints: [500, 1000],
      baselineName: 'CheckWindowWithLayoutBreakpointsInConfig',
    })
  })
  afterEach(async () => {
    await destroyDriver(driver)
    await eyes.abort()
  })
  it('check window with layout breakpoints in config', async () => {
    await spec.visit(
      driver,
      'https://applitools.github.io/demo/TestPages/JsLayout'
    )
    await eyes.open(
      driver,
      'Applitools Eyes SDK',
      'CheckWindowWithLayoutBreakpointsInConfig',
      undefined
    )
    await eyes.check({})
    await eyes.close(undefined)
  })
})
