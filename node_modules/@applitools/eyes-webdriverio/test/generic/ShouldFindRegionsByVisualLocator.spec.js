// should find regions by visual locator
const cwd = process.cwd()
const path = require('path')
const assert = require('assert')
const spec = require(path.resolve(cwd, 'src/spec-driver'))
const {testSetup, getTestInfo} = require('@applitools/sdk-shared')

describe('Coverage Tests', () => {
  let driver, destroyDriver, eyes
  beforeEach(async () => {
    ;[driver, destroyDriver] = await spec.build({browser: 'chrome'})
    eyes = testSetup.getEyes({
      vg: false,
      displayName: 'should find regions by visual locator ',
      baselineName: 'TestVisualLocators',
    })
  })
  afterEach(async () => {
    await destroyDriver(driver)
    await eyes.abort()
  })
  it('should find regions by visual locator ', async () => {
    await spec.visit(
      driver,
      'https://applitools.github.io/demo/TestPages/FramesTestPage/'
    )
    await eyes.open(
      driver,
      'Applitools Eyes SDK',
      'TestVisualLocators',
      undefined
    )
    const regionsMap = await eyes.locate({locatorNames: ['applitools_title']})
    await eyes.close(false)
    assert.deepStrictEqual(
      regionsMap,
      {applitools_title: [{left: 3, top: 19, width: 158, height: 38}]},
      undefined
    )
  })
})
